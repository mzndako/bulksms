## Installation

- Import data.sql in the /database folder into the database

- Config database.php in the /application/config folder to match your database details

- Create a new folder in  the root directory called "ci_sessions"

- Change the first database record in the domain table from mkalisms.com.ng to your own domain name.


